# shell脚本

**Shell 脚本（Shell Script）**就是把一系列 **Shell 命令写在一个文件里，让系统按顺序执行**。
 本质上它就是一个 **自动化执行命令的脚本文件**，通常用于 **自动化运维、部署、批处理任务**。

```sh
#!/bin/bash

echo "Hello World"
```



## 未来

实际上Python脚本比shell脚本更加易写。但是需要计算机需要带有python环境