# 增量更新

在Echarts实例创建之后，我们仍然可以更新option

```ts
myChart.setOption(option);
```

你可以只改其中一部分，其他保持不变：

```ts
// 只更新饼图 series
myChart.setOption({
  series: {
    id: 'pie',
    encode: {
      value: 3
    }
  }
});

```

- **不会重绘整个图表**

- **只更新指定 series / xAxis / yAxis 等**

- 这是 axisPointer 联动和动态切换列的基础