# 注解

在 **Java 注解中，数组值必须使用 `{}`，不能使用 `[]`**。这是 **Java 注解语法本身的规定**

Java 注解用来给代码添加“说明信息”，这些信息可以在**编译期、类加载期或运行期**被读取和处理。即注解是给编译器阅读的。

## 注解本质

注解本质就是一个 **特殊接口**

```
public @interface MyAnnotation {}
```

编译后其实是：

```java
public interface MyAnnotation extends Annotation {}
```

`Annotation`是所有注解的父类