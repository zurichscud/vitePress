# 泛型

**泛型不支持基本数据类型（如 `int`、`double`）**，只能使用**引用类型（对象）**。

泛型是在编译期实现的（**类型擦除**机制）：

```java
List<Integer> list = new ArrayList<>();
```

编译后会变成：

```java
List list = new ArrayList();
```

- 泛型类型信息在运行时被“擦除”

- JVM 只认识 **Object**

- 而基本类型（`int`）不是对象 ❌