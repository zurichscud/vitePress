# backdrop-filter

`backdrop-filter` 是一个**对元素“背后内容”生效的滤镜属性**，常用来做 **毛玻璃 / 玻璃拟态**

## 常用滤镜值

和 `filter` 支持的函数几乎一样：

```css
backdrop-filter: 
  blur(10px)
  brightness(1.2)
  contrast(1.1)
  saturate(1.5);

```

