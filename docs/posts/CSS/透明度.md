# 透明度

## alpha通道（部分透明）

```css
.box {
  background-color: rgba(0, 0, 0, 0.5);
}

```

- 只影响当前颜色
- 子元素不受影响
- 更常用、更安全 





## opacity（整体透明）

```css
.box {
  opacity: 0.5;
}
```



- 取值范围：`0 ~ 1`
- 整体透明：元素本身 + 所有子元素都会一起变透明**
- 例如子元素的文本颜色，背景颜色，都会被`opacity`控制